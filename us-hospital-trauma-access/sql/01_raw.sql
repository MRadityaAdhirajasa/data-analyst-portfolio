CREATE TABLE raw.hospitals (
  x text, y text, objectid text, id text, name text, address text,
  city text, state text, zip text, zip4 text, telephone text, type text,
  status text, population text, county text, countyfips text, country text,
  latitude text, longitude text, naics_code text, naics_desc text,
  source text, sourcedate text, val_method text, val_date text, website text,
  state_id text, alt_name text, st_fips text, owner text, ttl_staff text,
  beds text, trauma text, helipad text
);